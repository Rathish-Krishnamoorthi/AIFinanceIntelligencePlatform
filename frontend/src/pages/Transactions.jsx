import React from 'react'; import DataPage from './DataPage'; import RiskBadge from '../components/RiskBadge'; export default function Transactions() { return <DataPage title="Transactions" endpoint="/transactions" columns={[['transaction_id','ID'],['date','Date'],['category','Category'],['amount','Amount'],['transaction_type','Type'],['risk_level','Risk',(v)=><RiskBadge level={v}/>]]} />; }
